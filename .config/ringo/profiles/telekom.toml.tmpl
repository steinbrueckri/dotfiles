# Generated by render-ringo-profile.sh via `op inject` — do not edit or commit.
# This template holds 1Password secret references; the rendered file holds the
# resolved secrets. display_name intentionally reuses the username.
username     = "{{ op://Private/Telekom SIP/username }}"
auth_user    = "{{ op://Private/Telekom SIP/auth_user }}"
password     = "{{ op://Private/Telekom SIP/password }}"
domain       = "tel.t-online.de"
display_name = "{{ op://Private/Telekom SIP/username }}"
# UDP avoids the SRTP/"488 security violation" on outgoing calls that TLS
# triggers against the Telekom; this matches the previously working config.
transport    = "udp"
stun_server  = "stun:stun.t-online.de"
notify       = false
mwi          = false
regint       = 3600
